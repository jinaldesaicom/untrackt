# Google Drive Sync (optional)
# Create a Google Cloud Console project, enable the Google Drive API,
# add an OAuth 2.0 Web Client ID, and set the authorized JavaScript
# origins to your domain(s) (e.g. http://localhost:5173, https://untrackt.com).
VITE_GOOGLE_CLIENT_ID={Google API Client ID}
